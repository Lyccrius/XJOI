#include<bits/stdc++.h>
using namespace std;
int n, m, k, sid, P[200011], w[200011];
struct peop{
    int x, v;
}stf[200011];
int main(){
    freopen("transfer.in", "r", stdin);
    freopen("transfer.out", "w", stdout);
    scanf("%d", &sid);
    scanf("%d%d%d", &n, &k, &m);
    for(int i = 2; i <= n; i++){
        scanf("%d", &P[i]);
    }
    for(int i = 1; i <= k; i++){
        scanf("%d%d", &stf[i].x, &stf[i].v);
        if(stf[i].x == 1){

        }
    }
    while(m--){



    }
    return 0;
}