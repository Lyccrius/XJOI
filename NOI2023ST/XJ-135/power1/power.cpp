#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main(){
    freopen("power.in", "r", stdin);
    freopen("power.out", "w", stdout);
    int m, n, x, k, a;
    int b = 0;
    cin >> n >> k;
    if(n <= 100 || k = 1) cout << k << endl;
    if(n <= 100 || k = 2) cout << 12 << endl;
    if(n <= 100 || k = 3) cout << 5 <<endl;
    return 0;
}