#include<bits/stdc++.h>
using namespace std;
int n;
float a[1010][1010],f[1010];
int main() {
    freopen("tree.in", "r", stdin);
    freopen("tree.out", "w", stdout);
    cin >> n;
    for(int i = 1 ; i <= n ; i++) {
        cin >> a[i][x] >> a[i][y];
    }

    return 0;
}
//"Too hard! Isn't it?"