#include<bits/stdc++.h>
using namespace std;
int main()
{
    //freopen("power.in","r",stdin)
    //freopen("power.out","w",stdout)
    int n,i,j,k,a[N],cnt,ans=0;
    cin>>n>>k;
    if(k==1)
    {
        cout<<n<<endl;
        returm 0;
    }
    if(k==2)
    {
        ll ans=sqrt(n)
        rep(i,3,63)
        {
            int x=1;
            while(pow(x,1)<=n)
            {
                a[cnt++]=pow(x,i);
            }
        }
        sqrt(a+1,a+cnt+1);
        cnt=unique(a+1,a+cnt+1)-a;
        rep(i,1,cnt)
        {
            int x=sqrt+1;
        }
    }
    cout<<ans;
    return 0;
}