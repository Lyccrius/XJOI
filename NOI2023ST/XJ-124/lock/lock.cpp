#include <bits/stdc++.h>
using namespace std;
int t, k, n;

int main()
{
    freopen ("lock.in", "r", stdin);
    freopen ("lock.out", "w", stdout);
    cin >> t >> k;
    for (int i = 1; i <= t; ++ i)
    {
        cin >> n;
        for (int i = k; i <= n; ++ i)
        {
            int a, b;
            cin >> a >> b;
        }
        puts("0");
    }
    fclose(stdin);
    fclose(stdout);
}